{
    "tasks": [
        
        {
        "label": "Build and Run UI Framework",
        "type": "shell",
        "command": "cmake --build build && ./build/bin/main",
        "options": {
            "cwd": "${workspaceFolder}"
        },
        "group": {
            "kind": "build",
            "isDefault": true
        },
        "problemMatcher": [],
        "detail": "Build the project with CMake and run the compiled binary"
        }
        

    ],
    "version": "2.0.0"
}